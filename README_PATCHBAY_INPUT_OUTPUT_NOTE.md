# Patchbay parsing rule

- **Outputs** are parsed from the **first** matrix (top) in the sheet.
- **Inputs** are parsed from the **second** matrix (bottom) in the sheet (next 'Kanal' header below outputs).

This matches: *Input is always below, Output is always above*.
