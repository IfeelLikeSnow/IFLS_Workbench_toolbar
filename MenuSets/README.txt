IFLS Workbench Toolbar

REAPER assigns per-installation command IDs to scripts.
Therefore the real toolbar file is generated locally.

In REAPER: Actions -> Show action list -> run:
  IFLS_Workbench_Toolbar_Generate_ReaperMenu.lua

Then import the generated .ReaperMenu from this MenuSets folder.
