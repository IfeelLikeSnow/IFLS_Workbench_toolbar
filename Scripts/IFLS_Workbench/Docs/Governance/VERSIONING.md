# Versioning

We version 3 surfaces independently:

1) **Schema version**: breaking JSON schema changes (requires migration notes).
2) **Content version**: presets/devices/docs additions (compatible).
3) **Wizard version**: UI behavior.

Semantic Versioning guidance: declare public interface, bump MAJOR on breaking changes, MINOR on compatible features, PATCH on fixes.
